# # this is to avoid repeate code using matrix
# name: matxrix-fe-be
# on:
#   workflow_dispatch:
# jobs:
#   lint-frontend:
#       runs-on: ubuntu-latest
#       steps:
#       - name: checkout code
#         uses: actions/checkout@v7
#       - name: linter setup nodejs
#         uses: actions/setup-node@v6
#         with:
#           node-version: '20'
#           cache: npm
#           cache-dependency-path: frontend/package-lock.json
#       - name: install npm packages
#         run: npm ci
#         working-directory: frontend
#       - name: run linter
#         run: npm run lint
#         working-directory: frontend
#       - name: run tests
#         run: npm run test
#         working-directory: frontend
#      ## - Test
#      # - Push
#   lint-backend:
#       runs-on: ubuntu-latest
#       steps:
#       - name: checkout code
#         uses: actions/checkout@v7
#       - name: linter setup go
#         uses: actions/setup-go@v6
#         with:
#           go-version: '1.23'
#           go-version-file: 'go.mod'
#           check-latest: false
#           cache-dependency-path: backend/go.sum
#       - name: run go for fmt
#         run: go fmt
#         working-directory: backend
#       - name: rub for go vet
#         run: go vet
#         working-directory: backend
#       - name: run tests
#         run: go test
#         working-directory: backend

#   front-backend:
#     needs: [lint-frontend,lint-backend]
#     runs-on: ubuntu-latest
#     strategy:
#         matrix:
#           folder: ['frontend','backend']
#     steps:
#           - name: checkout code
#             uses: actions/checkout@v7
#           - name: docker login
#             uses: docker/login-action@v4
#             with:
#               username: ${{ secrets.DOCKER_USERNAME }}
#               password: ${{ secrets.DOCKER_PASSWORD }}
#           - name: docker build and push
#             uses: docker/build-push-action@v7
#             with:
#               context: ${{ matrix.folder }}
#               push: true
#               tags: ${{ secrets.DOCKER_USERNAME }}/devboard-${{ matrix.folder }}:latest
#   deploy:
#     needs: [lint-frontend,lint-backend,front-backend]
#     uses: ./.github/workflows/cd.yml
#     secrets: inherit
  